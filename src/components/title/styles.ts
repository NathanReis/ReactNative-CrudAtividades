import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    marginBottom: 16,

    fontSize: 24,
    fontWeight: 'bold',

    textAlign: 'center',
    textTransform: 'uppercase'
  }
});
