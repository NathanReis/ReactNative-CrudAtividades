import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    margin: 16,

    fontSize: 24,
    fontWeight: 'bold',
    color: '#36010c',
    textAlign: 'center',
    textTransform: 'uppercase'
  }
});
